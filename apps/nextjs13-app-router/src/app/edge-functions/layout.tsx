export const metadata = {
  title: "Edge Functions",
  description: "Edge Functions - Ask a question",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
